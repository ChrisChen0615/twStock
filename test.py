import numpy as np
import copy

# ary = np.array([['1', 11], ['3', 33], ['5', 55], ['2', 22], ['4', 44]])
# #ary  = np.sort(ary,axis=0)
# ary = ary[ary[:, 1].argsort()]
# mpObj = np.empty
# npObj = np.array(copy.deepcopy(ary)
#                  [:2], dtype=object)
# print(npObj)

# a = np.array([[3, 1, 2], [9, 8, 7]])
# b = np.array([[5, 6, 4], [6, 5, 4]])
# a = np.concatenate((a,b))
# print(a)
# a = np.sort(a,axis=0)
# print(a)

def main():
    return [1,2,3,4,5,6,7,8]
    
def calList():
    tList = main()
    return tList[:-1]



print(calList())



